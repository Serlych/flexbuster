const globalTransparency = '0.5'

const LightTheme = {
  primaryColor: `rgba(201, 216, 242, ${globalTransparency})`,
  secondaryColor: `rgba(176, 201, 244, ${globalTransparency})`,
  successColor: `rgba(191, 244, 176, ${globalTransparency})`,
  warningColor: `rgba(244, 228, 176, ${globalTransparency})`,
}

export default LightTheme;